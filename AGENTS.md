# Chat Relationship Analyzer — 产品说明与 Agent 协作规则

> 本文档面向产品经理、项目经理和参与协作的 Agent，说明产品做什么、用户如何使用以及必须遵守的业务边界。具体接口、数据结构、算法、安全实现和代码划分见 `SYSTEM_DESIGN.md`。

## 1. 产品定位

Chat Relationship Analyzer 是一款面向两人聊天记录的互动分析工具。用户粘贴当前支持的微信桌面端聊天复制文本后，产品分析当前聊天片段中可观察到的关系场景和双向互动倾向，并生成基于这些结果的关系解读。

产品分析的是聊天中表现出的投入程度、表达温度、回应协调和支持行为，不能读取任何人的真实内心，也不能确认现实关系身份、测量真实好感概率或作出心理诊断。

所有关系分类都必须表述为模型对当前聊天场景的匹配结果；综合互动指数只是产品内部的解释性指标，不代表关系质量或现实中的情感程度。

### 项目标识

- 代码目录名及包名：`chat-relationship-analyzer`
- 品牌名与 Logo：未定，页面暂用 `Chat Relationship Analyzer`
- 正式副标语：`Paste a conversation. See the interaction patterns.`

## 2. MVP 范围

第一版支持：

- 两位参与者、符合当前唯一支持结构的微信桌面端聊天复制文本；该结构必须在上线前通过代表性样例验收。
- 粘贴文本、本地解析、人工确认和单次在线分析。
- 主导关系场景匹配、符合条件时的关系细分、十个基础互动维度、由相关维度派生的互动平衡度与综合互动指数，以及关系解读。
- 用户在预览中明确选择 A 或 B 哪位是“我”；系统不默认猜测身份。
- 无需注册，不建立聊天历史。

第一版不支持：

- 登录、用户系统、历史报告和数据库。
- 文件导入、微信截图 OCR、图片或音频内容识别，以及当前唯一支持结构之外的其他微信复制格式。
- QQ、Telegram、任意 `昵称: 内容` 格式或多人群聊。
- 分享链接、付费系统和管理后台。
- 编辑解析后的昵称、消息归属、正文、时间或消息类型。
- 把模型结果描述成心理诊断、现实关系事实或真实好感概率。

### 三个核心功能

1. **关系场景与互动信号分析**：匹配当前聊天的主导关系场景，分析十个基础互动维度，并计算互动平衡度与综合互动指数。
2. **关系细分**：主导场景的置信度和概率差满足条件时，在该场景下进一步匹配关系细分。
3. **关系解读**：根据已经得到的场景匹配、互动维度和派生指标生成文字解读，不重新分类或评分。

## 3. 核心用户流程

```text
用户粘贴微信聊天复制文本
  → 浏览器本地解析
  → 用户预览解析结果并明确选择 A 或 B 哪位是“我”
  → 用户看到第三方处理提示并确认提交
  → 系统分析关系场景和互动信号
  → 页面立即展示关系场景匹配、互动维度、互动平衡度与综合互动指数
  → 系统继续生成关系解读
  → 页面在已有分析结果下方补充关系解读
```

解析预览是只读的。若解析结果有误，用户返回输入环节修改或重新粘贴原始文本；确认前不得产生外部模型费用。

A、B 仅按有效消息中发送者首次出现的顺序编号，不表示“我 / 对方”。预览页必须同时展示 A、B 的昵称和确认后的身份对应关系；未选择身份时不能提交分析。

互动信号分析与关系解读是两个真实、独立的阶段。关系解读失败时必须保留已经展示的互动结果，并允许用户单独重试；签名分析上下文无法继续使用时，统一提示重新运行完整分析，具体失败原因由服务端内部保留，不在前端拆分恢复流程。

## 4. 分析结果与业务边界

### 分析职责边界

```text
JEV
  ├─ 主导关系场景匹配
  ├─ 条件满足时进行关系细分
  └─ 十个基础互动维度

服务端公式
  ├─ 互动平衡度
  └─ 综合互动指数

DeepSeek
  └─ 根据上述既有结果生成关系解读
```

DeepSeek 不得重新分类、重新评分或推翻 JEV 的结果。

### 关系场景匹配

主导关系场景包括：浪漫或伴侣、朋友、家庭、工作或学校、交易或服务、弱关系或新联系人、其他、信息不足。

- 现实关系可能同时具有多个身份，结果只表示当前聊天片段最符合的主导场景。
- 只有主导场景具有足够置信度且与第二名拉开明显差距时，才展示关系细分。
- 主导场景与细分的概率必须分开展示，并称为“模型匹配分布”。
- 关系细分调用失败时，保留已经完成的主导场景和互动维度，并将细分显示为“暂时无法生成”；不得让细分失败导致整份互动分析失败。

### 互动分析

分析包含十个维度：双方各自的投入度和表达温度，以及回应协调、自我表达、支持关怀、未来导向、紧张敌意和权力不对等。

- 十个维度统一使用 0～4 的五级强度量表；“没有观察到明显信号”和“证据不足”必须是两个不同状态。
- 互动平衡度只描述当前样本中双方投入和温度是否相对均衡，不等于情感互惠或关系质量。
- 综合互动指数由正向互动维度组合而成；紧张敌意、权力不对等和互动平衡度单独展示，不进入综合指数。
- 证据不足时必须明确显示“证据不足”，不得强行给出数值或等级。
- 不得把综合指数包装成“对方喜欢你的概率”。

### 关系解读

- 关系解读只能解释已经得到的关系场景、匹配分布、置信度、互动维度、互动平衡度和综合互动指数。
- 不得重新分类、重新评分或推翻互动信号分析结果。
- 只能引用可以追溯到用户输入的聊天消息作为证据。
- 不输出行动建议、沟通策略、推荐话术或关系升级方案。
- 不诊断对方的心理、人格、精神状态或真实情感。

### 置信度表达

- 低置信度结果必须突出不确定性，不能只在脚注中弱化。
- `confidence` 表示模型分布是否集中，不表示答案正确率。
- 分类接近、证据不一致或未达到细分条件时，应解释不确定性本身，不强行得出明确关系结论。

## 5. 输入限制与异常体验

- 至少需要 8 条有效消息，并且双方各至少 2 条。
- 最多分析 500 条消息或 20,000 个规范化字符，以先达到者为准。
- 样本不足时不调用外部模型；超限时要求用户缩小聊天范围，不自动截断或抽样。
- 第一版只接受产品验收样例覆盖的单一微信桌面端复制结构；不符合时明确提示“暂不支持这种聊天复制格式”，不尝试宽松猜测。
- 关系解读失败不影响已经成功的互动信号分析。

## 6. 展示与交互原则

- 关系场景匹配、互动分析和关系解读必须分层展示。
- 关系场景区域明确标注“这是对当前聊天场景的模型匹配结果，不代表现实关系事实”。
- 关系解读区域明确标注“以下解读基于模型判断，不代表现实关系事实”。
- 正式分析使用水平分布条，不使用容易制造精确错觉的雷达图。
- 各维度允许显示“证据不足”。
- 报告页必须根据用户在预览中确认的身份，结合昵称明确说明参与者 A、B 分别代表“我”还是“对方”；不得默认 A 永远是“我”。
- 关系解读中的每条原文证据都应标明发送者 A 或 B；整体互动维度应标明“整体互动”，不得强行归给单方。
- “关键互动信号与原文证据”按“A 的分析”“B 的分析”“整体互动分析”分区展示；未选为关键维度的部分不得补造分析。
- 每条关键维度解读都应显示对应的互动维度名称，便于用户看出解读与维度结果的对应关系。
- 页面年轻、简约、有亲和力，但不幼稚或过度恋爱化。
- 使用暖白或浅灰背景、深灰正文、大留白、宽松行距、克制圆角和轻量边框，不使用大面积纯黑纯白。
- 全站采用低饱和莓果紫作为唯一主色系统；渐变只用于关系场景匹配的重点区域，不铺满页面。
- 动画只用于淡入、数值过渡和轻微状态切换，并遵循 `prefers-reduced-motion`。
- 移动端优先，关键按钮触控区域至少为 44×44 CSS px，并支持键盘操作和减少动态效果设置。
- 所有表单都有可见标签、键盘焦点和可理解的错误信息。

## 7. 隐私与内容原则

- 应用服务端不持久化聊天，不将聊天写入数据库、文件、缓存、队列、分析日志或错误追踪上下文。
- 消息正文不做脱敏；提交前必须说明消息会发送到 TypeSafe 和 DeepSeek，第三方处理与留存遵循其各自条款。
- 页面、错误信息和日志不得泄漏 API Key、聊天正文或完整模型响应。
- 出现威胁、控制、骚扰或自伤信号时，只说明观察到的风险信号及分析限制，不延伸为关系行动方案。

## 8. 产品验收标准

- [ ] 用户可以粘贴微信聊天文本，在本地预览解析结果并明确选择 A 或 B 哪位是“我”；未选择时不能提交。
- [ ] 确认前不产生外部模型费用，解析结果本身不可编辑。
- [ ] 互动信号分析完成后立即展示关系场景、互动维度、互动平衡度和综合互动指数。
- [ ] 两层匹配分布分开展示，不制造跨分支概率榜。
- [ ] 低置信度或样本不足时不输出确定结论。
- [ ] 综合指数被明确描述为产品内部指数，而非真实好感概率。
- [ ] 关系解读不改变既有分类、分数或概率，所有聊天证据均可追溯到输入。
- [ ] 关系解读不包含行动建议、推荐话术或关系升级方案。
- [ ] 关系解读失败时保留互动结果，并提供符合上下文有效期的恢复入口。
- [ ] 关系细分调用失败时保留主导场景与互动维度，并将细分标记为暂时不可用。
- [ ] 签名分析上下文无法继续使用时，前端统一引导用户重新运行完整分析；服务端保留具体失败原因用于诊断。
- [ ] 产品不持久化聊天，并如实披露第三方处理边界。

## 9. 尚待确认或上线前校准

- [ ] 品牌名和 Logo；MVP 暂用 `Chat Relationship Analyzer`。
- [ ] 使用人工构造且符合真实结构的样例，验证唯一支持的微信桌面端复制格式。
- [ ] 使用人工标注样本重新校准维度标准、权重和置信度阈值，并明确双方消息占比是否应作为互动平衡度的辅助门槛；完成校准前，消息占比只用于输入统计，不进入评分公式。
- [ ] 上线前核查域名和商标。

## 10. Agent 协作规则

- 开始设计、实现、测试或 Review 前，必须完整阅读本文档和 `SYSTEM_DESIGN.md`。
- 本文档是产品范围、用户流程和业务边界的事实来源；`SYSTEM_DESIGN.md` 是技术实现的事实来源，不在两处重复维护同一层级的细节。
- 用户最新的明确要求优先于现有文档；业务决策变化时更新本文档，技术实现变化时更新 `SYSTEM_DESIGN.md`，两者同时受影响时同步更新。
- 代码标识符使用英文；代码注释和 Git 提交信息尽量使用中文，技术专有名词除外。
